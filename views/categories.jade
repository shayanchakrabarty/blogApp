extends layout

block content
	.container
		.col-md-6
			h1 #{title}
			ul.list-group
				each category, i in categories
					li.list-group-item
						a(href="/articles/category/#{category._id}")	#{category.title}

